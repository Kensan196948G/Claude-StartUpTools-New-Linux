# reports/

ClaudeOS ループや診断で生成される一時レポートの置き場です。

## ルール

- `README.md` だけを Git で追跡します。
- 監査、CMDB、lint、coverage、deploy runbook などの実行結果は `.gitignore` で除外します。
- 恒久的な設計文書は `docs/`、運用方針は `CLAUDE.md` / `AGENTS.md` に置きます。
