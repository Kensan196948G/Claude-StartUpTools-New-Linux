# Eval Plan: <title>
対象: skill / agent instruction / workflow / routing policy / prompt

## Golden dataset（`tests/evals/*.json`）
## 比較指標: quality / correctness / regression / CI pass 率 / latency / token / cost / failure 率 / repair 率
## 変更前ベースライン
## 変更後結果と判定（悪化する変更は採用しない）
