{
  "_comment": "Claude Code スタートアップツール設定テンプレート v4.0.0-linux - コピーしてconfig.jsonとして使用",
  "version": "4.0.0-linux",
  "projects": "/home/kensan/Projects",
  "projectsDir": "/home/kensan/Projects",
  "autoInitProjects": false,
  "_autoInitProjects_comment": "true にすると start.sh 起動時に非Gitフォルダを git init する。通常起動の高速化と不要ログ抑制のため既定 false。",
  "_localExcludes_comment": "config_project_list の除外正本 (L1メニュー/cron/autonomy 全3経路に効く)。製品でないツール repo・バックアップ・アーカイブ・純インフラ等の git フォルダをここへ列挙して自動開発対象から外す。worktree (.git がファイル) も登録プロジェクトとして扱う。本ツール repo 自身 (CCSU_ROOT) は config-loader が自動除外するため記載不要。新規プロジェクトの cron 登録手順は docs/claude/11_新規プロジェクトのcron登録とオンボーディング.md を参照。",
  "localExcludes": [],
  "_projectGroups_comment": "実行スコープを特定のグループフォルダ配下だけに限定する場合に列挙 (例: [\"Mirai-DX-Project\",\"Mirai-Project\"])。設定すると config_project_list はその各グループ配下の Git リポジトリのみを「グループ名/サブ名」で列挙し (L1メニュー/cron/autonomy 共通)、メニューにはグループごとに L1..Ln が並び各行がそのグループ配下の選択へ直結する (配列の並び順 = L 番号)。S1 は従来どおり「グループ選択→配下選択」の2段階。グループ自身が .git を持っていても無視し配下を対象にする。autoInitProjects も設定時はグループ配下のみ対象。空配列 (既定) なら projects 直下全体が対象で L1 単独のフラット選択。",
  "projectGroups": [],
  "tools": {
    "defaultTool": "claude",
    "claude": {
      "enabled": true,
      "displayName": "Claude Code (Anthropic)",
      "command": "claude",
      "_args_comment": "PR-G で --dangerously-skip-permissions を撤去。権限は settings/auto-mode と headless の --permission-mode dontAsk へ委譲し、起動引数に権限ロジックを持ち込まない (多経路化回避)。",
      "args": [],
      "headless": {
        "_comment": "PR-G 新設。claude -p ヘッドレス既定運用の正本設定。cron.headlessDefault=true と整合。enabled=false で従来 TUI へ戻す (CLAUDEOS_HEADLESS=0 相当)。",
        "enabled": true,
        "outputFormat": "stream-json",
        "permissionMode": "dontAsk",
        "appendSystemPromptFile": "",
        "resume": true
      },
      "installCommand": "npm install -g @anthropic-ai/claude-code",
      "checkCommand": "claude --version",
      "env": {
        "ENABLE_TOOL_SEARCH": "true",
        "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1",
        "CLAUDE_CODE_ENABLE_PROMPT_SUGGESTION": "true"
      },
      "settings": {
        "language": "日本語",
        "outputStyle": "Explanatory",
        "alwaysThinkingEnabled": true,
        "spinnerTipsEnabled": true,
        "prefersReducedMotion": false,
        "promptSuggestionEnabled": true,
        "respectGitignore": true,
        "autoUpdatesChannel": "latest",
        "includeCoAuthoredBy": true,
        "teammateMode": "auto"
      },
      "apiKeyEnvVar": "ANTHROPIC_API_KEY"
    },
    "codex": {
      "enabled": false,
      "_disabled_reason": "Claude Code 専用のため無効化。将来復活の保険としてフィールドは保持。",
      "displayName": "Codex CLI (OpenAI)",
      "command": "codex",
      "args": [
        "--full-auto"
      ],
      "installCommand": "npm install -g @openai/codex",
      "checkCommand": "codex --version",
      "env": {
        "OPENAI_API_KEY": ""
      },
      "apiKeyEnvVar": "OPENAI_API_KEY"
    },
    "copilot": {
      "enabled": false,
      "_disabled_reason": "Claude Code 専用のため無効化。将来復活の保険としてフィールドは保持。",
      "displayName": "GitHub Copilot CLI",
      "command": "copilot",
      "args": [
        "--yolo"
      ],
      "installCommand": "npm install -g @github/copilot",
      "checkCommand": "copilot --version",
      "env": {},
      "apiKeyEnvVar": ""
    }
  },
  "logging": {
    "enabled": true,
    "logDir": "logs",
    "logPrefix": "ai-startup",
    "successKeepDays": 30,
    "failureKeepDays": 90
  },
  "backupConfig": {
    "enabled": true,
    "backupDir": "config/backups",
    "maxBackups": 10,
    "maskSensitive": true,
    "sensitiveKeys": [
      "tools.claude.env.ANTHROPIC_API_KEY",
      "tools.codex.env.OPENAI_API_KEY"
    ]
  },
  "recentProjects": {
    "enabled": true,
    "maxHistory": 10,
    "historyFile": "~/.ai-startup/recent-projects.json"
  },
  "notifications": {
    "_comment": "Linux 通知音設定。soundEnabled:false で無効化できます。",
    "soundEnabled": false,
    "playOnStart": true,
    "playOnExit": true,
    "sounds": {
      "claude":  "",
      "codex":   "",
      "copilot": ""
    }
  },
  "cron": {
    "_comment": "v3.1.0 新設。Linux 側 crontab で ClaudeCode を週次自動起動する機能の設定。",
    "enabled": true,
    "defaultDurationMinutes": 300,
    "maxDurationMinutes": 300,
    "maxProjectsPerDay": 2,
    "launcherPath": "/home/kensan/.claudeos/cron-launcher.sh",
    "sessionsDir": "/home/kensan/.claudeos/sessions",
    "logsDir": "/home/kensan/.claudeos/logs",
    "entryPrefix": "CLAUDEOS",
    "_headlessDefault_comment": "PR-G で既定反転。true = cron 起動は claude -p ヘッドレス既定 (CLAUDEOS_HEADLESS 未設定=1 と整合)。false で TUI 退避へ戻す。tmux は --tmux または CLAUDEOS_TMUX=1 明示時のみ。",
    "headlessDefault": true
  },
  "supervisor": {
    "_comment": "省クレジット運用の全体デフォルト。project/state.json の supervisor ブロックでプロジェクト別に上書き可能。foregroundSessionMinutes は L1 (対話 TUI) 専用で 0 = 無制限。sessionMinutes (S1/BG 自律) とは独立。",
    "defaults": {
      "dailyMaxMinutes": 600,
      "maxRestartsPerDay": 1,
      "sessionMinutes": 300,
      "foregroundSessionMinutes": 0,
      "cooldownSeconds": 30,
      "crashLoopThreshold": 3,
      "crashLoopMinSeconds": 120
    }
  },
  "agentSdk": {
    "_comment": "PR-E 新設。claude -p / Agent SDK headless の月次クレジット可視化・上限ガード設定 (2026-06-15 課金変更対応)。monthlyBudgetUsd=0 は無制限 (ガード無効)。Supervisor は project/state.json に個別予算が無い場合、この値を停止ガードに使う。warn/verifyOnly/stop は消費率(%)の段階閾値で CLAUDE.md §9 と同型。ledgerPath は credits__record の追記先 (~ 展開は credits.sh が CCSU_HOME 基準で解決)。",
    "monthlyBudgetUsd": 300,
    "warnPct": 70,
    "verifyOnlyPct": 85,
    "stopPct": 95,
    "ledgerPath": "/home/kensan/.claudeos/credits/ledger.jsonl"
  },
  "modelRouter": {
    "_comment": "既定は全タスク Opus 5 (2026-08-06 ユーザー指示)。高リスク (security/architecture/design/release 等) は xhigh、通常タスクは high (Opus 5 の recommended 既定)。Sonnet 5 は task に 'sonnet' を含めるか CLAUDEOS_MODEL_KEY=sonnet の明示 opt-in のみ。",
    "enabled": true,
    "_balanceEnabled_comment": "利用量バランス機構 (低利用モデルへの自動切替)。既定 false — 有効化すると約半数のセッションが Sonnet へ流れ『既定 = Opus 5』と両立しないため opt-in。",
    "balanceEnabled": false,
    "balanceThresholdPct": 5,
    "usageFile": "~/.claudeos/model-usage.jsonl",
    "models": {
      "opus": { "id": "claude-opus-5" },
      "sonnet": { "id": "claude-sonnet-5", "effort": "max" }
    },
    "_models_comment": "opus.effort は未設定なら高リスク=xhigh / 通常=high の2階層を自動適用。固定したい場合のみ明示 (例: { \"id\": \"claude-opus-5\", \"effort\": \"high\" })。",
    "_taskEffort_comment": "フェーズ別 effort 上書き (opt-in)。キーが task 文字列に部分一致したら effort を上書き。値は low|medium|high|xhigh|max のみ有効。空 {} = 既定階層 (高リスク xhigh / 通常 high / sonnet=max) を維持。例: { \"monitor\": \"medium\" } で Monitor フェーズのみ軽量化。",
    "taskEffort": {}
  },
  "email": {
    "_comment": "v3.2.0 新設。Cron セッション終了時に HTML レポートメールを送信。SMTP 認証情報は Linux 環境変数で管理 (CLAUDEOS_SMTP_USER / CLAUDEOS_SMTP_PASS) — config.json には書かない。送信トリガーは Linux 側 CLAUDEOS_EMAIL_ENABLED=1 で明示的に有効化。詳細は docs/common/16_HTMLメールレポート設定.md を参照。",
    "enabled": false,
    "smtp": {
      "host": "smtp.gmail.com",
      "port": 587,
      "useStartTls": true,
      "userEnvVar": "CLAUDEOS_SMTP_USER",
      "passEnvVar": "CLAUDEOS_SMTP_PASS"
    },
    "from": "<your-email>",
    "to": "<your-email>",
    "subjectPrefix": "[ClaudeOS]",
    "scriptPath": "/home/kensan/.claudeos/report-and-mail.py",
    "enableEnvVar": "CLAUDEOS_EMAIL_ENABLED"
  },
  "sessionTabs": {
    "_comment": "libexec/watch-session.sh による headless セッション情報表示。tmux セッション表示は --tmux 明示時のみ。",
    "enabled": true,
    "title": "Claude Session Info",
    "pollIntervalSeconds": 1,
    "autoCloseAfterExitSeconds": 10,
    "localSessionsDir": "~/.claudeos/sessions"
  },
  "statusline": {
    "_comment": "v3.1.0 新設。メニュー 13 で実行される、グローバル statusLine 設定を Linux 側に一括同期する設定。",
    "enabled": true,
    "sourceSettingsPath": "~/.claude/settings.json",
    "targetSettingsPath": "~/.claude/settings.json",
    "backupBeforeApply": true
  }
}
